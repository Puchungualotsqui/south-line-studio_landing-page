---
import Link from "@/components/ui/link.astro";
---

<section id="contact" class="mt-28">
  <div class="mx-auto max-w-3xl">
    <h2 class="text-3xl lg:text-4xl font-bold tracking-tight">
      Get in touch
    </h2>

    <p class="text-lg mt-4 text-slate-600">
      If you'd like a website or have a quick question, leave your details below.
      I'll reply by email or phone.
    </p>
  </div>

  <div class="mx-auto max-w-3xl mt-10">
    <form
      action="https://formspree.io/f/xwvozwjz"
      method="POST"
      class="grid gap-5"
    >
      <!-- Honeypot (spam protection) -->
      <input type="text" name="_gotcha" class="hidden" tabindex="-1" autocomplete="off" />

      <div class="grid sm:grid-cols-2 gap-5">
        <div>
          <label for="contact-phone" class="block text-sm font-medium text-slate-700 mb-1">
            Phone
          </label>
          <input
            id="contact-phone"
            type="tel"
            name="phone"
            required
            autocomplete="tel"
            class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-slate-900"
          />
        </div>

        <div>
          <label for="contact-email" class="block text-sm font-medium text-slate-700 mb-1">
            Email
          </label>
          <input
            id="contact-email"
            type="email"
            name="email"
            required
            autocomplete="email"
            class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-slate-900"
          />
        </div>
      </div>

      <div>
        <label for="contact-business" class="block text-sm font-medium text-slate-700 mb-1">
          Type of business
        </label>
        <input
          id="contact-business"
          type="text"
          name="business"
          placeholder="Electrician, café, clinic, etc."
          autocomplete="organization"
          class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-slate-900"
        />
      </div>

      <div>
        <label for="contact-service" class="block text-sm font-medium text-slate-700 mb-1">
          What do you need?
        </label>
        <select
          id="contact-service"
          name="service"
          class="w-full rounded-md border border-slate-300 px-3 py-2 bg-white focus:outline-none focus:ring-2 focus:ring-slate-900"
        >
          <option value="Landing page">Landing page</option>
          <option value="Hosting & maintenance">Hosting & maintenance</option>
          <option value="Landing page + hosting">Landing page + hosting</option>
          <option value="Other">Other</option>
        </select>
      </div>

      <div>
        <label for="contact-message" class="block text-sm font-medium text-slate-700 mb-1">
          Message (optional)
        </label>
        <textarea
          id="contact-message"
          name="message"
          rows="4"
          class="w-full rounded-md border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-slate-900"
        ></textarea>
      </div>

      <div class="flex flex-col sm:flex-row gap-4 items-start">
        <button
          type="submit"
          class="cursor-pointer inline-flex items-center justify-center rounded-md bg-slate-900 px-6 py-3 text-white font-medium hover:bg-slate-800 transition"
        >
          Send message
        </button>

        <p class="text-sm text-slate-500 max-w-sm">
          Or call directly if you prefer. No obligation.
        </p>
      </div>
    </form>

    <div class="mt-6 text-sm text-slate-500">
      Prefer calling?
      <a href="tel:+61493940206" class="underline underline-offset-4 ml-1">
        Call now
      </a>
      or
      <a
        href="mailto:southlinestudio.au@gmail.com"
        class="underline underline-offset-4 ml-1"
      >
        email
      </a>
    </div>
  </div>
</section>
